# Pinned to the versions the app is built and tested against.
# PySide6-Essentials (not full PySide6) — the app only uses QtCore/Gui/Widgets.
PySide6-Essentials==6.11.1
opencv-python==5.0.0.93
onnxruntime==1.27.0
numpy==2.4.6
Pillow==12.3.0
